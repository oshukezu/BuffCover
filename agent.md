/bash /browser-agent
我們要在剛剛成功的環域分析 Web App 基礎上進行「版本升級（V2）」。請保留原本的 Leaflet 地圖與半徑調整功能，並使用輕量化的 Python（如 Geopandas/Shapely）或前端 JS 擴充以下三個核心商業功能：

1. 地址地理編碼（Geocoding）：
   - 在前端介面上方增加一個「核心起始點地址」輸入框。
   - 當使用者輸入特定門牌號碼（例如：台中市西屯區台灣大道三段99號）並點擊「定位」時，系統必須將地址轉換為經緯度，並將地圖中心與環域圓圈自動移至該點。
   - (備註：請先使用免 API Key 的開源 OSM Nominatim API 進行地址解析，若失敗則提供手動輸入經緯度的備用方案)。

2. 大量參考點匯入（Batch Import）：
   - 在介面上增加一個「匯入參考地址」的區塊。
   - 支援兩種匯入方式：
     (A) 讓使用者直接在文字框內「一行一個地址」貼上多個門牌號碼。
     (B) 預設內建一組 10 個台中市西屯區的模擬門牌地址（作為測試 Demo 數據）。
   - 系統在背景將這些地址批次解析為經緯度，並以「紅色小大頭針（Markers）」標記在地圖上。

3. 空間交集與涵蓋率計算（Spatial Overlay & Coverage Rate）：
   - 核心邏輯：當使用者拉動動態滑桿（Slider）調整環域半徑時，系統必須即時計算「目前圓圈範圍內，圈中了多少個匯入的參考地址」。
   - 介面呈現：在側邊欄或地圖上方即時顯示計算結果：
     * 匯入總地址數：X 個
     * 環域內涵蓋地址數：Y 個
     * 核心指標「地址涵蓋率」：(Y / X) * 100 %
   - 進步視覺化：落入環域圓圈內的標記自動變成「綠色」，圓圈外的保持「紅色」。

完成代碼修改後，請自動啟動 /browser-agent，模擬輸入 3 個測試地址，並確認拉動滑桿時，涵蓋率的百分比與大頭針顏色會動態即時更新。